<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="AVF.MemberManagement.Views.PasswordPage">
    <StackLayout VerticalOptions="Center" HorizontalOptions="Center">
        <Label Text="Neues Passwort:"></Label>
        <Entry IsPassword="True" Text="{Binding Password1}"></Entry>
        <Label Text="Passwort wiederholen:" IsVisible="{Binding IsValidPassword1}"></Label>
        <Entry IsPassword="True"  Text="{Binding Password2}" IsVisible="{Binding IsValidPassword1}"></Entry>
        <Button Text="Speichern und Weiter" Command="{Binding SaveAndContinueCommand}"></Button>
    </StackLayout>
</ContentPage>