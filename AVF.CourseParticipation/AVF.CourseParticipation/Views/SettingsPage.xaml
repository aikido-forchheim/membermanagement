<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="AVF.CourseParticipation.Views.SettingsPage">
    <StackLayout VerticalOptions="Center">
        <StackLayout Orientation="Horizontal">
            <Label>URL:</Label>
            <Entry Placeholder="https://api.homepage.com/controller" Keyboard="Url" Text="{Binding ApiUrl}"></Entry>
        </StackLayout>
        <StackLayout Orientation="Horizontal">
            <Label>Benutzer:</Label>
            <Entry Placeholder="Benutername" Keyboard="Email" Text="{Binding Username}"></Entry>
        </StackLayout>
        <StackLayout Orientation="Horizontal">
            <Label>Passwort:</Label>
            <Entry Placeholder="Passwort" IsPassword="true" Keyboard="Text" Text="{Binding Password}"></Entry>
        </StackLayout>
        <Button Text="Einstellungen prüfen" Command="{Binding TestCommand}"></Button>
        <Button Text="Speichern" Command="{Binding SaveCommand}"></Button>
        <Label Text="{Binding Message}"></Label>
    </StackLayout>
</ContentPage>